{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <div class="container px-4 px-lg-5">
        <h1 class="mt-4">{{ category.name }}</h1>
        <p class="text-muted">ID: {{ category.id }}</p>
        
        <div class="mb-4">
            <h4>Description</h4>
            <p>{{ category.description|default('Pas de description.') }}</p>
        </div>
        
        <a href="{{ path('app_category_index') }}" class="btn btn-secondary">Retour à la liste</a>
        <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="btn btn-primary">Modifier</a>
        {{ include('category/_delete_form.html.twig') }}
    </div>
{% endblock %}
