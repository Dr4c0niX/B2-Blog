{% extends 'base.html.twig' %}

{% block title %}Modifier le Commentaire{% endblock %}

{% block body %}
    <div class="container px-4 px-lg-5">
        <h1 class="mt-4">Modifier le Commentaire</h1>
        <div class="card p-4">
            {{ form_start(form, {'attr': {'class': 'mt-4', 'enctype': 'multipart/form-data'}}) }}
                <div class="mb-3">
                    {{ form_label(form.content, 'Contenu', {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.content, {'attr': {'class': 'form-control', 'rows': 5}}) }}
                    {{ form_errors(form.content) }}
                </div>
                <button type="submit" class="btn btn-primary">Modifier</button>
            {{ form_end(form) }}
        </div>
        <a href="{{ path('app_post_show', {'id': post.id}) }}" class="btn btn-secondary my-3">Retour à l'article</a>
        {{ include('comment/_delete_form.html.twig', { 'comment': comment }) }}
    </div>
{% endblock %}
